/** SVG drawing surface — terrain, sprinklers and pipes render here (M2+). */
export function Canvas() {
  return (
    <svg width="100%" height="100%" style={{ display: 'block', background: '#fafafa' }}>
      <text x="50%" y="50%" textAnchor="middle" fill="#999">
        Terrain editor coming in M2
      </text>
    </svg>
  )
}
