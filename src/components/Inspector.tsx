/** Right-side panel — properties of the selected point/segment/sprinkler (M2+). */
export function Inspector() {
  return (
    <aside style={{ width: 260, borderLeft: '1px solid #ddd', padding: 12 }}>
      <em style={{ color: '#999' }}>Nothing selected</em>
    </aside>
  )
}
