import type { Design } from './model/types'

/**
 * Paste an exported design JSON here (use "Copy" in the toolbar) to ship it
 * as the app's initial configuration. Leave null to start with a blank design.
 */
export const initialConfig: Design | null = null
